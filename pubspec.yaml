name: flutter_image_multi_grid_selection
description: Image Multi Grid Selector is a Flutter package that allows users to select a single image from a grid layout and provides a shade overlay on the selected grid.
version: 0.0.1
license: MIT
homepage: https://github.com/asif46/flutter_image_multi_grid_selection
repository: https://github.com/asif46/flutter_image_multi_grid_selection.git
environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
